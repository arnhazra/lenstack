{
  "name": "arcstack",
  "version": "7.2.3",
  "description": "a saas product",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix apps/apigateway\" \"npm run dev --prefix apps/emailservice\" \"npm run dev --prefix apps/uiservice\"",
    "publish": "git add . && git commit -m publish && git push"
  },
  "author": "Arnab Hazra",
  "license": "GNU AFFERO GENERAL PUBLIC LICENSE",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}