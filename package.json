{
  "name": "arcstack",
  "version": "8.0.0",
  "description": "arcstack",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/ui\"",
    "publish": "git add . && git commit -m publish && git push"
  },
  "author": "Arnab Hazra",
  "license": "GNU GENERAL PUBLIC LICENSE",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}